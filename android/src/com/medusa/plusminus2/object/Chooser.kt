package com.medusa.plusminus2.`object`

import com.badlogic.gdx.graphics.Color
import com.badlogic.gdx.graphics.Texture
import com.badlogic.gdx.graphics.g2d.Batch
import com.badlogic.gdx.graphics.g2d.GlyphLayout
import com.badlogic.gdx.graphics.g2d.Sprite

class Chooser(val positive:Boolean) {

//    var number = 0
//        set(value) {
//            glyph.setText(font, (if (positive) "" else "-") + value.toString())
//        }
//
//    override val fieldSprite = Sprite(Texture("sprites/Chooser.png")).apply { color = if (positive) Color.RED else Color.BLUE }
//    private val font = SceneManager.getFont("fonts/ranga.ttf")?.apply { }
//    private val glyph = GlyphLayout()
//
//    init {
//        width = fieldSprite.width
//        height = fieldSprite.height
//    }
//
//    override fun draw(batch: Batch?, parentAlpha: Float) {
//        fieldSprite.draw(batch)
//        font?.draw(batch, glyph, x + (width - glyph.width) / 2, y + (width + glyph.height) / 2)
//    }

}